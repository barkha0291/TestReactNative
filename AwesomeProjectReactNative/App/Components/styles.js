// @flow

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    textNormal15BoldWhite: {
        fontSize: 15,
        fontWeight: '500',
        color: '#FFFFFF',
    }
})

